version: "3.8"
services:
  aipo:
    build: .
    volumes:
      - $(pwd):/aipo
    depends_on:
      - mongo

  mongo:
    image: mongo
    restart: always
    ports:
      - "8083:8092"
    volumes:
      - $(pwd)/BD/data:/BD/data

